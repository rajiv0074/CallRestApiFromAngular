import { CallApiResponseObject } from './call-api-response-object';

describe('CallApiService', () => {
  it('should create an instance', () => {
    expect(new CallApiResponseObject()).toBeTruthy();
  });
});
